// Entrypoint declared by agent.manifest.json.
// Scaffold only — the example agent is not implemented.

export async function run(): Promise<void> {
  throw new Error('Not implemented (scaffold only)')
}
