# @agent-transit/api

Backend API. Scaffold only — route files under `src/routes/` document intended endpoints; nothing is implemented.

Cross-cutting concerns (TODO): authentication, workspace membership, entitlement checks, Stripe webhooks, platform event/audit logging.
