// Catalog endpoints. Scaffold only — not implemented.
//
// GET  /templates                 browse/search; filter by outcome, role, category, trust level, price
// GET  /templates/:slug           template detail page data
// GET  /templates/:slug/versions  version history; pinnable versions

export {}
