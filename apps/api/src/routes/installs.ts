// Workspace install endpoints. Scaffold only — not implemented.
//
// POST /installs                  install free template or entitled paid template into workspace
// GET  /installs                  list workspace installs with setup/update state
// POST /installs/:id/pin          pin a template version
// GET  /installs/:id/export       entitlement-gated ZIP export
// GET  /installs/:id/deploy       deployment instructions (default target: Vercel)

export {}
