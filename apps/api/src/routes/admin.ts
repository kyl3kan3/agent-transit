// Admin endpoints. Scaffold only — not implemented.
//
// GET  /admin/review-queue        first-template submissions and warning-level scans
// POST /admin/reviews/:id         approve / reject / block a version
// POST /admin/creators/:id/verify verify a creator
// CRUD /admin/collections         collections and taxonomy
// POST /admin/moderation          moderate reviews and templates
// POST /admin/refunds/:id         handle refunds and disputes
// GET  /admin/events              platform events and marketplace analytics

export {}
