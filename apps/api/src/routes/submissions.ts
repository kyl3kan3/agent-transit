// Creator submission endpoints. Scaffold only — not implemented.
//
// POST /submissions               submit public repo URL or ZIP package
// POST /submissions/validate      validate a manifest before review
// GET  /submissions/:id           scan results and review status
// POST /submissions/:id/publish   publish per routing rules (auto / first-template review / admin review)

export {}
